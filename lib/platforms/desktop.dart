import 'package:flutter/material.dart';

import '../features/auth/view/desktop_login.dart';
class DeskTop extends StatelessWidget {
  const DeskTop({super.key});

  @override
  Widget build(BuildContext context) {
    return const DesktopLogin();
  }
}
